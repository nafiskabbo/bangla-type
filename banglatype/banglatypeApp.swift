//
//  banglatypeApp.swift
//  BanglaType — SwiftUI used for Preferences window only (NSHostingView).
//  Entry point is main.swift + AppDelegate.
//

import SwiftUI

struct BanglaTypeApp: App {
    var body: some Scene {
        WindowGroup {
            ContentView()
        }
    }
}
